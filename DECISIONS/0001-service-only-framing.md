# ADR 0001 — Service-Only Framing

## Status

Accepted

## Decision

FA Local is implemented as a bounded service/library crate. It must not broaden into a UI surface, generic orchestrator, or semantic authority layer.
