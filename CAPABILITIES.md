# Capability Admission

Capability admission is separate from request validity.

The scaffold defines shared capability-related vocabulary and denial primitives. Registry loading, revocation handling, and requester-class admission checks arrive in the next implementation tickets.
